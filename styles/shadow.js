import { StyleSheet } from "react-native";

const Shadows = StyleSheet.create({
  main: {
    shadowColor: "#000",
    shadowOffset: {
      width: 0,
      height: 3
    },
    shadowOpacity: 0.2,
    shadowRadius: 4.65,

    elevation: 6
  }
});

export default Shadows;
